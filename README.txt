BEN - GITHUB PACKAGE

Extract this ZIP first.

Upload these extracted items to the ROOT of:
KurtDhylanMotoShopInventoryV3

.github/workflows/build-android.yml
KurtDhylanMotoShopInventoryV3_Source.zip
README.txt

Then open GitHub Actions and run:
Build Kurt Dhylan Moto Shop Inventory V3 APK

The workflow automatically finds the Source ZIP, extracts it, finds the
.csproj (not the .sln), installs MAUI Android, builds the APK, and uploads
the APK as an artifact.
